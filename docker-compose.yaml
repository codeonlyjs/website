services:
  server:
    build:
      context: .
    environment:
      NODE_ENV: production
    ports:
      - 3105:3000
    volumes:
      - ./data/:/usr/src/app/data/
    restart: unless-stopped
    